# 2026-05-06

## 项目全面代码审查与 Bug 修复

修复了 AI Trending 项目中 5 个关键问题：

### 已修复
1. **时间筛选器失效（核心问题）** — `server/src/routes/projects.js` 将过滤字段从 `fetched_at` 改为 `trending_range`，使"今天/本周/本月/全部"筛选正确工作。
2. **Skills 自动刷新失效** — `client/src/pages/RecommendPage.tsx` 将局部变量改为 `useRef`，修复了爬虫完成后前端不自动刷新列表的 bug。
3. **Skill 详情页 404** — 创建独立 `SkillDetailPage.tsx` 使用 `useSkillDetail` hook，更新 `App.tsx` 路由，给 `SkillCard` 添加 `<Link>` 包裹。
4. **Trending 数据 open_issues_count 语义错误** — `server/src/services/scraper.js` 第 86 行，`open_issues_count` 不再错误映射为 `currentPeriodStars`，改为 0。
5. **.env API 密钥明文** — 确认项目不是 Git 仓库，密钥未泄露到版本控制，但提醒注意本地安全。

### 已知遗留
- ~~`gitterapp.com` 第三方 API 依赖风险~~ → 已用 GitHub 官方 Search API 替代
- DeepSeek 和 GitHub 密钥建议定期轮换
